const hello = 'Hello'
const world = 'world'
// Template Literals
let epicPhrase = `${hello} ${world}!`;
console.log(epicPhrase)

// Multiline strings

let lorem = `A la verga
estoy escribiendo
en varias lineas`;
console.log(lorem);